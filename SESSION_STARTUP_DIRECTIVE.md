# 3R Session Startup Directive
**Project: 3R** — [Project description TBD]

---

## Purpose
This document defines the mandatory startup protocol for every Claude Code session on the 3R project. Follow every step before beginning any implementation work.

---

## Startup Protocol

### Step 1: Read Context Files
Read the following files at the start of every session:
- `MASTER_TODO.md` — current phase and open tasks
- `MEMORY.md` (in `.claude/projects/C--Users-Chris-Kremer-Documents-GitHub-3R/memory/`) — accumulated project knowledge
- Any relevant session notes in `docs/sessions/` for recent context

### Step 2: Check Project Health
Run the following to establish baseline status:
```bash
git status
git log --oneline -10
```
Add any project-specific build/test commands here once the stack is finalized (e.g., `npm run build`, `dotnet build`, `pytest`, etc.).

Note any build errors, test failures, or uncommitted changes before proceeding.

### Step 3: Identify Current Phase
Determine which phase of `MASTER_TODO.md` is active and what tasks are in progress or blocked.

### Step 4: Produce Startup Summary
Before beginning work, output a summary in this format:

```
## Session Startup Summary — 3R
- Build status: [PASS / FAIL — describe errors]
- Git status: [clean / N uncommitted files]
- Current branch: [branch name]
- Current phase: [Phase X — Name]
- Active tasks: [list from MASTER_TODO]
- Blocked tasks: [list, with reason]
- Planned work this session: [brief description]
```

---

## Project Context

**3R** — [Full project description to be added here once defined.]

### User Types / Roles
| Role | Description |
|---|---|
| TBD | Define roles as the project is scoped |

### Core Feature Areas
- TBD — define feature areas during Phase 1 (Architecture & Design)

### Technology Stack
| Layer | Technology |
|---|---|
| **Backend** | TBD |
| **Frontend** | TBD |
| **Database** | TBD |
| **Auth** | TBD |
| **Hosting** | TBD |
| **CI/CD** | GitHub Actions |

---

## Repository Structure

| Path | Purpose |
|---|---|
| `.claude/` | Claude Code config and session settings |
| `docs/` | Architecture specs, API contracts, design docs, session notes |
| `.github/workflows/` | GitHub Actions CI/CD pipelines |
| `scripts/` | Utility and automation scripts |
| `src/` | Application source code |
| `data/` | Data files, seeds, samples, exports |
| `MASTER_TODO.md` | Phased development task tracker |
| `SESSION_STARTUP_DIRECTIVE.md` | This file — session startup protocol |

---

## Critical Rules

1. **No sensitive data in commits** — no connection strings, secrets, or API keys in source
2. **Conventional commits** — use `feat:`, `fix:`, `chore:`, `docs:`, `test:`, `refactor:` prefixes
3. **No AI attribution** — do not add "Generated by Claude" or similar to code or docs
4. **Ask before deleting** — never delete files, migrations, or database objects without confirmation
5. **Small, focused commits** — commit logically grouped changes; avoid giant all-in-one commits
6. **Branch discipline** — feature work on `kremer-dev` or feature branches; `main` is protected
7. **Update MASTER_TODO after every session** — keep phase status and task completion accurate

---

## Branching Strategy

| Branch | Purpose |
|---|---|
| `main` | Stable, production-ready code only |
| `kremer-dev` | Primary development branch |
| `feature/*` | Individual feature branches off `kremer-dev` |
| `fix/*` | Bug fix branches |

---

## Session End Protocol

Before closing a session:
1. Commit any completed work with a conventional commit message
2. Update `MASTER_TODO.md` — mark completed tasks, add newly discovered tasks
3. Write a session note to `docs/sessions/YYYY-MM-DD-brief-title.md`
4. Update `MEMORY.md` with any new stable patterns, decisions, or lessons learned
5. Push current branch to remote: `git push origin kremer-dev`
